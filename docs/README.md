# docs

Architecture documentation for Nabla AI.

- [`adr/`](adr/) — Architecture Decision Records (ADRs). Start with
  [the index](adr/README.md).

The authoritative, enforced architecture rules live in the repository-root
[`CLAUDE.md`](../CLAUDE.md). ADRs explain *why* those rules exist and capture
the decisions behind them.
